已知我后端调用的代码如下：
import requests


def query_zhongyi_fastapi(input: str) -> str:
    url = "http://127.0.0.1:8000/process"
    payload = {"input": input}

    res = requests.post(url, json=payload)
    json_dict = res.json()
    return json_dict["output"]


帮我用streamlist写一个前端代码，能实现和机器人的交互对话功能，你帮我实现一下吧。